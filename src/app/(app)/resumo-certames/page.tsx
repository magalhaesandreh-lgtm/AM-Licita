import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function ResumoCertamesPage() {
  return (
    <div className="container mx-auto">
      <Card>
        <CardHeader>
          <CardTitle>Resumo de Certames</CardTitle>
        </CardHeader>
        <CardContent>
          <p>Em construção (Fase X)</p>
        </CardContent>
      </Card>
    </div>
  );
}
